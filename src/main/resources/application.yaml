spring:
  application:
    name: pnc-parcial-final-hotel

  datasource:
    url: jdbc:postgresql://localhost:5432/ParcialFinalNCapas
    username: ${DB_USERNAME:postgres}
    password: ${DB_PASSWORD:WMJM261202261202jm--}
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect

server:
  port: 8081

app:
  jwt:
    secret: ${JWT_SECRET:miClaveSecretaSuperLargaParaJWTQueDebeTener256Bits!!}
    expiration-ms: 900000        # 15 minutos
    refresh-expiration-ms: 604800000  # 7 días